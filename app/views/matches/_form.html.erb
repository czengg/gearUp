<%= form_for(@match) do |f| %>
  <% if @match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% @match.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--   <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :player_one_id %><br>
    <%= f.number_field :player_one_id %>
  </div>
  <div class="field">
    <%= f.label :player_two_id %><br>
    <%= f.number_field :player_two_id %>
  </div>
  <div class="field">
    <%= f.label :sport_id %><br>
    <%= f.number_field :sport_id %>
  </div> -->



  <div class="field">
    <%= f.label :winner_id %><br>
    <%= f.select :winner_id, [[Player.where(:user_id => @match.player_one_id).first.first_name, @match.player_one_id], [Player.where(:user_id => @match.player_two_id).first.first_name, @match.player_two_id]] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
